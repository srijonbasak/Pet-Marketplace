{
  "name": "pet-marketplace-root",
  "private": true,
  "scripts": {
    "build": "cd pet-marketplace-frontend && npm install && npm run build"
  },
  "dependencies": {
    "lottie-react": "^2.4.1"
  }
}
